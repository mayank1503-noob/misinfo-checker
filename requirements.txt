fastapi
uvicorn
python-multipart
flask                                    # backend/api/flask_app.py: serves the frontend and takes uploads
python-dotenv
requests
pydantic
trafilatura
htmldate
pytest

# claim extraction transformer backend (optional; falls back to regex without them)
# on Windows/CPU: pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers
torch

# stage 3a: the evidence graph
networkx
pyvis                                    # optional: export_html only

# stage 3b: evidence retrieval
PyYAML                                   # config/sources.yaml
faiss-cpu                                # optional: the seed index falls back to numpy
sentence-transformers                    # shared by the seed index and stage 4 ranking
numpy

# stage 5: image evidence
Pillow                                   # image loading, demo placeholder images
torchvision                              # required by DINOv2's image processor
